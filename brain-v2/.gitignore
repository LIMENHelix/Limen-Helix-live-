# Runtime artifacts. The log and snapshots are REGENERATED by any run and must not be
# committed: they are megabytes per run, and a stale committed log would look like history.
state/

# The cached fixture IS committed. It is real recorded data (a pull of /api/feed-record) and
# every test depends on it being byte-identical, which is what makes the replay deterministic.
!fixtures/
