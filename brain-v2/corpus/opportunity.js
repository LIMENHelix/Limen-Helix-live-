/**
 * HALTED PROTOTYPE — intentionally not an opportunity engine.
 *
 * Opportunity promotion is Layer 4 and remains unauthorized/unbuilt. The experimental
 * implementation depended on the rejected adapter and is retained in Git history at
 * c7db1bf5. Importing this path must fail before any ranking or promotion can occur.
 */
'use strict';

throw new Error(
  'HALTED_PROTOTYPE: brain-v2/corpus/opportunity.js is not an active opportunity engine. ' +
  'Layer 4 requires separate authorization; see CORPUS_CONTRACT.md.'
);
