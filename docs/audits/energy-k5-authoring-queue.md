# Energy K5 — Human Authoring Workbench Feed

**Date:** 2026-06-18  **Probe:** `scripts/_taxonomy-pilot/k5-energy-authoring-queue.cjs` (read-only build)
**Queue:** `assets/data/deep/energy-authoring-queue.json` (135 KB, 156 tasks)
**Verdict:** The unusable deep substrate is now an **actionable authoring queue** — what to author from real
sources, never fabricated content.

## 156 tasks, by type

| Authoring type | Count | Priority |
|---|---|---|
| methodCandidate needed | 3 | 1 |
| embodimentCandidate needed | 3 | 1 |
| figurePlaceholder needed | 3 | 1 |
| source citation needed (portal rehydration) | 136 | 2 |
| ticker relevance verification needed | 11 | 3 |

By diagnosis: OIL_SHOCK 28 · NUCLEAR_INCIDENT 28 · SYSTEMIC_ENERGY_STRESS 36 · GRID_COLLAPSE 25 ·
PIPELINE_DISRUPTION 16 · RENEWABLE_INTERMITTENCY 12.

## Priority 1 — external-source bundle field gaps (the evidence layer)

OIL_SHOCK, NUCLEAR_INCIDENT, SYSTEMIC_ENERGY_STRESS each need **method + embodiment + figure** authored
(9 tasks). These mirror the brain's J2 `treatmentContext.authoringIntake` and carry the same real source
hints (EIA STEO / IEA OMR / OPEC; IAEA INES / NRC / 10 CFR; NERC / FERC / EIA). Completing them is what
lets conscience lift its patent/grant veto for those diagnoses.

## Priority 2 — deep-portal rehydration (replace fake authority)

136 DANGEROUS diagnosis-branch portals (from K2/K3). Each task: replace templated/evidence-graded-but-
unsourced treatments with sourced content, **or** demote the portal to a context-only label. Existing
content is recorded as `unusableTemplateContent` so the author sees what to discard.

## Priority 3 — ticker relevance verification

11 context portals (L0/L1) where company tickers are real but node-binding is templated. Verify each
ticker actually operates in the sub-domain (10-K / filings) — verification can promote a ticker from
context to supporting evidence; until then it stays context-only.

## Each task carries

`target · diagnosis · level · authoringType · missingFields · existingContext · unusableTemplateContent ·
sourceHints · requiredHumanAction · priority · whyItMatters`.

## Integrity + J2 link

- **Nothing fabricated** — tasks record *what is needed*, never invented candidate content (9/9 acceptance).
- **Mirrors J2** — the brain already emits `treatmentContext.authoringIntake` for external bundles at
  runtime; this file is the persistent, full-corpus queue version. When a human authors a field and it is
  wired into a bundle, the J2 intake for that diagnosis empties and conscience readiness can advance.

## Acceptance (9/9)

3 external-bundle gaps represented · 3 fields each · synthetic portals became tasks (not evidence) ·
ticker tasks present · no fabricated content · J2 source hints mirrored · rehydration count = K3 (136) ·
compact (135 KB) · doc present.

## Next: K6 — branch-by-branch rehydration plan (what would make each branch evidence-eligible).
