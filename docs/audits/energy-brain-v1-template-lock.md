# Energy Brain v1 — Template Lock

**Date:** 2026-06-18  **Gate:** `scripts/_taxonomy-pilot/energy-template-lock-probe.cjs` (non-zero-exit)
**Canonical guard:** `scripts/_taxonomy-pilot/_portal-real-content-classifier.cjs`

This freezes the completed Energy brain as the **reusable domain-brain standard** before Finance begins.

## Final label (locked)

> Energy is a **functioning recurrent, source-grounded, immune, aware, conscience-gated,
> intuition-capable, simulation-capable domain brain v1 — with cortex-depth honesty enforced.**
>
> It has real cognition and source discipline, but it does **not** yet have organic deep portal cortex.
> Its deep evidence layer comes from **verified external bundles + human-authoring intake**, not from
> L1/L2 portal traversal.

The four mature behaviors that define the standard:
1. Learns from prediction error. 2. Protects itself from synthetic substrate.
3. Knows what it cannot support. 4. Creates authoring work-queues instead of faking completion.

## The 18-point template standard (all enforced by the lock probe)

1. Recurrent model exists. 2. Prediction error updates prior. 3. DDP survives to finalizer.
4. PromptView compact. 5. Source coverage explicit. 6. External-source bundles human-verification-required.
7. Alias mappings preserve `aliasRisk` + `aliasReviewStatus`. 8. Immune quarantines synthetic portal material.
9. Awareness reports knowns/unknowns/suppressions. 10. Conscience blocks unsupported artifact readiness.
11. Intuition makes hunches but **cannot** become evidence or diagnosis. 12. Simulation hypothetical only.
13. Executive report compact. 14. Portal cortex depth must be **proven** before traversal.
15. Company names alone do **not** qualify portal content as real. 16. Template/mad-lib treatments quarantined.
17. Missing candidate fields route to human-authoring intake. 18. Non-energy domains unchanged.

## The reusable rule (executable, not aspirational)

Encoded in `_portal-real-content-classifier.cjs` and enforced by the lock:

```
A domain may not claim cortex depth unless its portal layer passes the real-content classifier.
Company names alone do not make a portal real  (MIXED != depth; only REAL with low template ratio qualifies).
Template treatments are quarantined.
Synthetic portal depth cannot feed evidence, bundles, readiness, or finalizer claims.
Missing candidate fields become human-authoring intake, not generated facts.
```

`qualifiesAsRealDepth()` returns true **only** for `REAL` files (tmplRatio < 0.4 and realTreatments > 0).
A `MIXED` file (real companies + template treatments) returns false — names alone never qualify.

**Drift guard:** the runtime `energy-brain.js` carries an inline mirror of the mad-lib verb list (browser
IIFE can't `require()`). The lock probe parses the runtime list and asserts it contains every canonical
verb — so the detector that nearly let synthetic L1 through (J1) can never silently regress.

## What is REUSABLE for Finance (copy the shape)

- The phase recipe: **A → B → F0 → F1 → F2 → F3 → G1/b/c/d → C1 → G2 → H1–H7 → J1/J2/J3 → Lock.**
- The architecture shapes: recurrent `energyModel` loop; the 8-section DDP + compact `promptView`;
  the 6 H-layers (immune/awareness/conscience/intuition/simulation/executive) as **domain-level** state
  embedded per-diagnosis; canonical aliasing with risk/review metadata; the finalizer-survival lane.
- The integrity invariants: no fabrication; external bundles `human-verification-required`; intuition
  never promotes to evidence/diagnosis; simulation hypothetical-only.
- **The classifier itself** — `_portal-real-content-classifier.cjs` is shared; Finance imports it.
- The probe/gate discipline: per-phase targeted probe + 5 critical gates + full regression at boundaries;
  non-zero-exit gating; never commit on echo-only "PASS".

## What must be RE-AUDITED per domain (never assumed)

- **Whether the domain's portal cortex is real or synthetic.** Run the classifier on the domain's L0/L1
  FIRST. Energy's L0 was REAL, L1/L2 synthetic — Finance may differ in either direction. Do **not** assume
  Energy's portal correction applies identically.
- The domain's real diagnosis set + canonical aliases + which external institutions are authoritative.
- Which diagnoses have real source bundles vs. need external authoring (the G1/G1d split is per-domain).
- The H-layer specifics: immune antigens, conscience veto conditions, simulation scenario types, and
  intuition analogy-families are **domain-specific** — re-derive, don't copy Energy's literals.

## What must NEVER be blindly copied

- Energy's mad-lib verb literals as "the answer" — they are the classifier's *current* list; a new domain
  may use a different template vocabulary. Extend the **canonical** classifier, never fork it per domain.
- Energy's diagnosis→branch map, alias targets, scenario/analogy families, source-hint strings.
- The assumption that "real companies present" = "real portal." It does not. Always run the classifier.
- Any claim of cortex depth without a passing `qualifiesAsRealDepth()` result.

## Acceptance (all green)

18/18 template standard + 8/8 acceptance guards + child re-runs of `j-energy-real-depth-probe` and
`h7-energy-functioning-brain-final-audit` (both exit 0) + runtime↔classifier drift-free. Full Energy
regression: 14 probes pass (gated). **Energy Brain v1 is locked. Finance may begin — starting with a
portal real-vs-synthetic audit, not an assumption.**
