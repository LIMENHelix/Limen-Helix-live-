# Energy Deep Portal Rescue — Final Audit (K7)

**Date:** 2026-06-18  **Probe:** `scripts/_taxonomy-pilot/k7-energy-deep-portal-rescue-final.cjs`
**Result:** K0–K7 complete; truth layer operational; **nothing synthetic admitted as evidence.**

## The core question, answered operationally

> "Why have deep portals if we cannot get there?"

**We *can* get there — and that was never the problem.** The 26,967 deep Energy portals (L3–L6) are
reachable in production via `/api/fetch-portal` (GitHub Contents API; proven HTTP 200 at every depth).
The problem is **admissibility**: 0 of the 27,160 portals are real evidence. So the answer is operational,
not philosophical:

- **Keep** the portal tree — as **navigation / context / authoring substrate**, not evidence.
- **Never** admit L2–L6 as evidence (100% DANGEROUS authority-masquerade).
- **Use** the curated external-source bundles as the sole evidence layer.
- **Convert** select branches to real evidence on demand via the authoring queue (K5) + rehydration plan (K6).
- **Don't** enable live deep traversal until a branch passes `classifyPortalV2` as REAL.
- **Prune** only a branch that is genuinely abandoned — otherwise it stays as context/authoring substrate.

## The seven-line summary the operator asked for

| Question | Answer |
|---|---|
| Which portals exist? | 27,160 (L0 1 · L1 19 · L2 173 · L3 829 · L4 7,401 · L5 18,485 · L6 252) |
| Which are unreachable? | None — all reachable via `/api/fetch-portal` |
| Which are reachable but inadmissible? | L2–L6 (26,967) — DANGEROUS / authority-masquerade |
| Which are usable as context? | 11 (L0 + 10 company-bearing L1), relevance-unverified |
| Which are usable as evidence? | **0 portals.** Evidence = 6 curated external-source bundles only |
| Which require human authoring? | 156 queued tasks (9 bundle field-gaps + 136 rehydration + 11 ticker checks) |
| What before live traversal? | A branch must pass `classifyPortalV2` → REAL (provenance + companies + non-template) |
| Keep / clean / rebuild / prune? | **Keep** as context/authoring substrate; **rebuild** select branches via K6; prune only if abandoned |

## What the rescue built (K0–K7)

- **K0** substrate located (full repo, reachable via API, intentionally excluded from static).
- **K1** access map (tree coherent: 0 orphans, 0 dup, 1 missing branch root, 388 navigational gaps).
- **K2** reality classifier v2 (6-way; 0 REAL, deep = DANGEROUS authority-masquerade).
- **K3** certified cortex index (machine-readable: evidenceEligible / contextOnly / blockedDangerous / needsRehydration).
- **K4** safe retrieval layer (`energy-cortex-retrieval.js`, inert library; evidence = external bundles only, synthetic blocked).
- **K5** authoring queue (156 real-gap tasks; nothing fabricated; mirrors J2 intake).
- **K6** rehydration plan (branch-by-branch; recommended first branch = NUCLEAR; source-building vs code-wiring split).
- **K7** this audit.

## Verification (14/14)

K0–K6 artifacts present · 0 portals admitted as evidence · evidence layer = 6 external bundles · safe
retrieval returns no synthetic as evidence · authoring queue carries real gaps · **critical gates + H7
still pass** (template-lock) · K4 gate passes · final doc present.

## Can Energy proceed with C2 (live deep traversal)?

**No — and that is the correct, deliberate state.** Deep traversal stays blocked until a branch is
rehydrated to REAL. The infrastructure to allow it the moment a branch qualifies is in place (K3 index +
K4 retrieval), so enabling traversal becomes a per-branch consequence of authoring, not a separate project.

## Can Energy start the Finance template transfer?

**Yes.** The Energy domain brain is complete and locked (template-lock v1), and the portal-truth lane is
now fully resolved (K0–K7). Finance can begin — and per the template lock, its **first step is to run the
real-content classifier on Finance's own portal layer**, not to assume Energy's synthetic finding transfers.

## Status

**Energy deep-portal rescue COMPLETE (K0–K7).** Per the lane plan, the Energy lane is finished; pause here.
Rehydration (K6) is the on-demand follow-on when the operator wants to author a branch (start: NUCLEAR).
