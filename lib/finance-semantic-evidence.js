'use strict';

/* Compatibility entry point for Finance consumers. The transport grammar is
 * domain-neutral; Finance still owns its own packet, manager and authority. */
module.exports = require('./domain-semantic-evidence');
