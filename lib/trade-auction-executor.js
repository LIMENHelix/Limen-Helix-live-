'use strict';var Decision=require('./trade-auction-decision.js'),Motor=require('./product-domain-motor-authorization.js');var SCHEMA='trade-auction-command/1.0',LOG_KEY='trade_auction_command_log',PENDING_KEY='trade_auction_pending_log',PREFIX='trade_auction_command:',ACTION='trade_auction_action:',MOTOR='trade_auction_motor_claim:',BUDGET='trade_auction_budget_slot:',SUPPRESSION_KEY='trade_auction_suppression_catalog';function commandKey(id){return PREFIX+id;}function actionKey(id){return ACTION+id;}function motorKey(id){return MOTOR+id;}function held(reason,x){return Object.assign({ok:true,status:'HELD',accepted:0,reason:reason,providerCalls:0,productDomain:'trade',ownerDomain:'supplyChain',lane:'auction',liveMoney:false},x||{});}async function save(s,c){await s.set(commandKey(c.commandId),c);var r=await s.get(commandKey(c.commandId));if(!r||r.status!==c.status||r.actionId!==c.actionId)throw new Error('trade auction command readback invalid');return r;}async function slot(s,actionId,slots,now,cost){var day=new Date(now).toISOString().slice(0,10);for(var i=1;i<=slots;i++){var k=BUDGET+day+':'+i,v={schemaVersion:SCHEMA,actionId:actionId,day:day,slot:i,estimatedCostUsd:cost,claimedAt:now},made=await s.setIfAbsent(k,v),r=await s.get(k);if(r&&r.actionId===actionId)return{ok:true,slot:i,duplicate:!made};}return{ok:false,reason:'trade-auction-daily-budget-exhausted'};}async function execute(i){i=i||{};var s=i.store,v=i.candidate,d=i.decision,now=Number(i.now)||Date.now();if(!Decision.validateReceipt(d,v,now))return held('trade-auction-exact-b10-decision-required');var cost=Number(i.operationCostUsd),budget=Number(i.dailyBudgetUsd),cap=Math.max(0,Math.min(1,Number(i.dailyListingCap)||0));if(i.operationCostUsd==null||!Number.isFinite(cost)||cost<0)return held('trade-auction-operation-cost-not-configured');if(!cap)return held('trade-auction-daily-listing-cap-zero');if(cost>0&&(!Number.isFinite(budget)||budget<cost))return held('trade-auction-daily-dollar-budget-not-configured-or-too-small');var slots=Math.min(cap,cost===0?cap:Math.floor((budget+1e-12)/cost));try{s.assertDurable();var sup=await s.get(SUPPRESSION_KEY);if(sup&&sup[v.assetRefHash]&&sup[v.assetRefHash].suppressed)return held('trade-auction-asset-suppressed');var auth=await(i.motorAuthorization||Motor).authorize(s,'supplyChain','auction',now);if(!auth||!auth.authorized)return held(auth&&auth.reason||'trade-auction-motor-held',{motorReceiptId:auth&&auth.receiptId||null});var prior=await s.get(actionKey(d.actionId));if(prior)return Object.assign({ok:prior.status==='LISTED',accepted:prior.status==='LISTED'?1:0,replayed:true},prior);var commandId='tac_'+Decision.hash({action:d.actionId,motor:auth.receiptId}).slice(0,24),c={schemaVersion:SCHEMA,commandId:commandId,actionId:d.actionId,decisionReceiptId:d.decisionReceiptId,status:'COMMANDING',productDomain:'trade',ownerDomain:'supplyChain',lane:'auction',productMotorReceiptId:auth.receiptId,listingRequestId:v.listingRequestId,marketplaceId:v.marketplaceId,sellerId:v.sellerId,assetRefHash:v.assetRefHash,contentHash:v.contentHash,evidenceHash:v.evidenceHash,predictedOutcome:d.predictedOutcome,assetRightsConfirmed:true,bindingSaleAuthorized:false,orderAcceptanceAuthorized:false,paymentAuthorized:false,operationCostUsd:cost,dailyBudgetUsd:cost===0?0:budget,dailyListingCap:cap,providerCalls:0,commandedAt:now,liveMoney:false};if(!await s.setIfAbsent(commandKey(commandId),c))return s.get(commandKey(commandId));c=await s.get(commandKey(commandId));if(!c||c.status!=='COMMANDING')throw new Error('trade auction pre-dispatch readback invalid');if(!await s.setIfAbsent(motorKey(auth.receiptId),{schemaVersion:SCHEMA,commandId:commandId,productMotorReceiptId:auth.receiptId,claimedAt:now})){c.status='REFUSED';c.reason='trade-auction-motor-receipt-already-consumed';return save(s,c);}var mc=await s.get(motorKey(auth.receiptId));if(!mc||mc.commandId!==commandId)throw new Error('trade auction motor claim readback invalid');var bs=await slot(s,d.actionId,slots,now,cost);if(!bs.ok){c.status='HELD_BUDGET';c.reason=bs.reason;return save(s,c);}c.budgetSlot=bs.slot;await s.lpush(PENDING_KEY,c);await s.ltrim(PENDING_KEY,0,999);var a={schemaVersion:SCHEMA,actionId:d.actionId,commandId:commandId,status:'DISPATCHING',claimedAt:now};if(!await s.setIfAbsent(actionKey(d.actionId),a))return s.get(actionKey(d.actionId));c.status='DISPATCHING';c=await save(s,c);if(!i.marketplace||typeof i.marketplace.createListing!=='function')throw new Error('trade auction marketplace adapter missing');var result;try{result=await i.marketplace.createListing({marketplaceId:v.marketplaceId,sellerId:v.sellerId,title:v.title,price:v.reservePriceUsd,description:v.description,category:v.category,condition:v.condition,quantity:v.quantity,saleMode:'auction',assetRef:v.assetRef,assetRefHash:v.assetRefHash,contentHash:v.contentHash,auctionEndsAt:v.auctionEndsAt,bindingSaleAuthorized:false,orderAcceptanceAuthorized:false,paymentAuthorized:false});}catch(e){result={error:String(e&&e.message||e),ambiguous:true};}c.listingId=result&&result.id||null;c.publicPath=c.listingId?'/api/relay-marketplace?action=list-listings&marketplaceId='+encodeURIComponent(v.marketplaceId):null;c.status=c.listingId?'LISTED':result&&result.error?'AMBIGUOUS':'FAILED';c.failure=!c.listingId?String(result&&result.error||'listing-unresolved').slice(0,240):null;c.resolvedAt=Date.now();c.durableReceiptReadbackVerified=true;c.publicEffectExecuted=c.status==='LISTED';a.status=c.status;a.listingId=c.listingId;a.resolvedAt=c.resolvedAt;await s.set(actionKey(d.actionId),a);var ar=await s.get(actionKey(d.actionId));if(!ar||ar.status!==c.status)throw new Error('trade auction action receipt readback invalid');c=await save(s,c);await s.lpush(LOG_KEY,c);await s.ltrim(LOG_KEY,0,999);return Object.assign({ok:c.status==='LISTED',accepted:c.status==='LISTED'?1:0},c);}catch(e){return{ok:false,status:'REFUSED',accepted:0,reason:'trade-auction-strict-boundary-unavailable',detail:String(e&&e.message||e),providerCalls:0,liveMoney:false};}}
module.exports={SCHEMA:SCHEMA,LOG_KEY:LOG_KEY,PENDING_KEY:PENDING_KEY,SUPPRESSION_KEY:SUPPRESSION_KEY,commandKey:commandKey,actionKey:actionKey,motorKey:motorKey,execute:execute};
