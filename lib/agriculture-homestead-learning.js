'use strict';

var SCHEMA = 'agriculture-homestead-learning/1.0';
var EXTERNAL = 'product-domain-external-learning/1.0';
var STATE_KEY = 'agriculture_homestead_learning_state';
var CAUSE_PREFIX = 'agriculture_homestead_learning_cause:';
function causeKey(id) { return CAUSE_PREFIX + id; }
function fresh() { return { schemaVersion: SCHEMA, domain: 'agriculture', lane: 'homestead', resolvedCount: 0, signals: [], processedObservationIds: [] }; }
async function load(store) { var state = await store.get(STATE_KEY); if (!state) return fresh(); if (state.schemaVersion !== SCHEMA || !Array.isArray(state.signals) || !Array.isArray(state.processedObservationIds)) throw new Error('agriculture homestead learning state malformed'); return state; }
async function save(store, state) { await store.set(STATE_KEY, state); var restored = await store.get(STATE_KEY); if (!restored || restored.resolvedCount !== state.resolvedCount) throw new Error('agriculture homestead learning readback invalid'); return restored; }
async function recordCommand(store, command) { var cause = { schemaVersion: SCHEMA, domain: 'agriculture', lane: 'homestead', actionId: command.actionId, commandId: command.commandId, decisionReceiptId: command.decisionReceiptId, predictedOutcome: command.predictedOutcome, commandedAt: command.commandedAt }; var created = await store.setIfAbsent(causeKey(command.actionId), cause); var restored = await store.get(causeKey(command.actionId)); if (!restored || restored.commandId !== command.commandId) throw new Error('agriculture homestead cause readback invalid'); return { ok: true, duplicate: !created }; }
async function recordObservation(store, observation) { if (!observation || observation.status !== 'COUNTERPARTY_RESPONSE_OBSERVED' || !observation.observationId || !observation.actionId) return { ok: false, reason: 'independent-counterparty-response-required' }; if (!(await store.get(causeKey(observation.actionId)))) return { ok: false, reason: 'agriculture-homestead-action-cause-missing' }; var state = await load(store); if (state.processedObservationIds.indexOf(observation.observationId) >= 0) return { ok: true, duplicate: true }; var signal = { schemaVersion: EXTERNAL, signalId: 'els_' + observation.observationId, eventId: observation.observationId, actionId: observation.actionId, ownerDomain: 'agriculture', lane: 'homestead', eventType: 'OUTCOME_HOMESTEAD_COUNTERPARTY_RESPONSE', observedAt: observation.observedAt, outcome: 'counterparty-response', normalizedCredit: 0.5, sourceKind: 'independent-action-outcome', sourceIdentity: { kind: 'resend-signed-inbound-email', value: observation.providerInboundEmailId } }; state.signals.push(signal); state.signals = state.signals.slice(-200); state.processedObservationIds.push(observation.observationId); state.processedObservationIds = state.processedObservationIds.slice(-2000); state.resolvedCount++; state.lastOutcomeAt = observation.observedAt; await save(store, state); return { ok: true, signal: signal, resolvedCount: state.resolvedCount }; }
async function readForBrain(store) { var state = await load(store), signal = state.signals.length ? state.signals[state.signals.length - 1] : null, ids = {}; state.signals.forEach(function (item) { if (item.sourceIdentity) ids[item.sourceIdentity.kind + ':' + item.sourceIdentity.value] = true; }); var distinct = Object.keys(ids).length; return { schemaVersion: EXTERNAL, domain: 'agriculture', status: signal ? 'ELIGIBLE' : 'ABSTAINED', reason: signal ? null : 'domain-has-no-graded-external-action-outcome', resolvedCount: state.resolvedCount, learningGate: { ready: state.resolvedCount >= 5 && distinct >= 2, minimumResolved: 5, distinctSources: distinct, minimumDistinctSources: 2 }, signal: signal }; }
module.exports = { SCHEMA: SCHEMA, STATE_KEY: STATE_KEY, causeKey: causeKey, recordCommand: recordCommand, recordObservation: recordObservation, readForBrain: readForBrain };
