'use strict';

/** Backward-compatible import name. The registry now holds every sovereign
 * product-domain subscriber lane except Finance and Religion, whose custom
 * implementations remain separate. */
module.exports = require('./sovereign-domain-subscriber-lanes.js');
